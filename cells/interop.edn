[{:cell/id "interop", :cell/glyph "墨壺", :cell/kind :langgraph, :cell/runtime :wasm, :db/id -1, :cell/gates ["G1" "G4"], :cell/doc "Cleanroom translator: a Vectorworks (VectorScript) or AutoCAD (ObjectARX/.NET / command) shaped script → neutral ModelOp list. Python mirror of the TS adapters. Uses ONLY published call shapes (G1); unsupported tokens are skipped honestly (G4).", :cell/handler "agent.handle_interop", :cell/kotoba "{:reads [], :writes [:dwg.entity/id :dwg.entity/kind :dwg.entity/layer]}"}]
